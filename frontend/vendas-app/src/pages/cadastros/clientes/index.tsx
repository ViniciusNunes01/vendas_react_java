import { CadastroCliente, RotaAutenticada } from "@/components";

export default function () {
    return (
        <RotaAutenticada>
            <CadastroCliente />
        </RotaAutenticada>
    )
}