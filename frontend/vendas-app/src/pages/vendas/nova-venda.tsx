import { RotaAutenticada } from "@/components"
import { Vendas } from "../../components/vendas"

export default function () {
    return (
        <RotaAutenticada>
            <Vendas />
        </RotaAutenticada>
    )
}